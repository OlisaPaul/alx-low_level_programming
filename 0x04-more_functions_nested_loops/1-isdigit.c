#include "main.h"

/**
 * _isupper - define if a character is a number
 *
 * Description: Prints the alphabet with _putchar
 * @c: charater
 * Return: void
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
		return (1);
	else
		return (0);
}

#include <string.h>

/**
 * _strlen - returns the length of a string
 *
 * Return: int
 */
void _puts(char *str)
{
	int i;

	for(i = 0; i < strlen(str); i++)
	{
		_putchar(str[i]);
	{
	_putchar('\n');
}
